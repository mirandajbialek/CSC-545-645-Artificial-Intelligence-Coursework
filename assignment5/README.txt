Hello!

This is my assignment 5.

By default, the algorithm will solve for the map of the entire (50) states. However, 
the 10 state Southeastern map can be uncommented and ran as well. 

For the min-conflicts, of course it does not always find a solution. To show how it works, I made an example of 
it running on 50 states as well as the 10 states as it is much more likely to find example within the max iterations 
using the 10 state version.  

My function that computes the average time takes a parameter specifying the number of iterations to 
run each algo. For now, I pass the integer 5 so that the entire program will run almost instantaneously. 
I figure this makes it easier for grading purposes. However, it will still run within 1 minute for the 
100 iteration version if you choose to run it as such.

This program can be run by running the run.sh file with the command:

./run.sh

in the assignment5 folder. 

Best, 
Miranda Bialek